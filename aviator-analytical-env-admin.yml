spruce:
  - base: ci/analytical-env-admin_emr/meta-analytical-env-admin_emr.yml
    prune:
      - meta
    merge:
      - with_in: ci/analytical-env-admin_emr/
        regexp: ".*yml"
      - with_in: ci/analytical-env-admin_emr/jobs/admin_emr/
        regexp: ".*yml"
      - with_in: ci/analytical-env-admin_emr/jobs/rbac_admin/
        regexp: ".*yml"
      - with_in: ci/analytical-env-admin_emr/jobs/taint/
        regexp: ".*yml"
      - with_in: ci/analytical-env-admin_emr/jobs/terminate-cluster/
        regexp: ".*yml"
      - with_in: ci/shared/
        regexp: "meta.yml"
      - with_in: ci/shared/
        regexp: "resourcetypes.yml"
      - with_in: ci/shared/update-pipeline/
        regexp: "update-pipeline-resources.yml"
      - with_in: ci/shared/update-pipeline/
        regexp: "update-pipeline-groups.yml"
    to: aviator_pipeline_analytical_env_admin.yml
fly:
  name: analytical-env-admin_emr
  target: utility
  config: aviator_pipeline_analytical_env_admin.yml
  expose: false
  check_creds: true
