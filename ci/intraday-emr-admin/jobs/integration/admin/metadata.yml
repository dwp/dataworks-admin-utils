jobs:
  - name: integration-remove-metadata
    plan:
      - .: (( inject meta-intraday-emr-admin.plan.remove-metadata ))
        config:
          params:
            AWS_ROLE_ARN: arn:aws:iam::((aws_account.integration)):role/ci
            AWS_ACC: ((aws_account.integration))
            CLUSTER_ID: ""
