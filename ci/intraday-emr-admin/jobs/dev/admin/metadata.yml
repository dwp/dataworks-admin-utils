jobs:
  - name: dev-remove-metadata
    plan:
      - .: (( inject meta-intraday-emr-admin.plan.remove-metadata ))
        config:
          params:
            AWS_ROLE_ARN: arn:aws:iam::((aws_account.development)):role/ci
            AWS_ACC: ((aws_account.development))
            CLUSTER_ID: ""
