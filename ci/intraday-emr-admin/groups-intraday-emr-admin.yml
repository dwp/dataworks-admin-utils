groups:
  - name: admin_emr-start-cluster
    jobs:
      - dev-start-cluster
      - qa-start-cluster
      - integration-start-cluster
      - preprod-start-cluster
      - production-start-cluster

  - name: admin_emr-stop-cluster
    jobs:
      - dev-stop-cluster
      - qa-stop-cluster
      - integration-stop-cluster
      - preprod-stop-cluster
      - production-stop-cluster

  - name: remove-metadata
    jobs:
      - dev-remove-metadata
      - qa-remove-metadata
      - integration-remove-metadata
      - preprod-remove-metadata
      - production-remove-metadata

  - name: trigger-cluster
    jobs:
      - dev-trigger-cluster
      - qa-trigger-cluster
      - integration-trigger-cluster
      - preprod-trigger-cluster
      - production-trigger-cluster
