jobs:
  - name: prod-start-cluster
    plan:
      - .: (( inject meta-mongo-latest-emr-services.plan.start-cluster ))
        config:
          params:
            AWS_ROLE_ARN: arn:aws:iam::((aws_account.production)):role/ci
            AWS_ACC: ((aws_account.production))
            S3_PREFIX: test
            CORRELATION_ID: test
            EXPORT_DATE: "2021-04-01"
