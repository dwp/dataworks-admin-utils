jobs:
  - name: qa-stop-cluster
    plan:
      - .: (( inject meta-clive-emr-services.plan.stop-cluster ))
        config:
          params:
            AWS_ROLE_ARN: arn:aws:iam::((aws_account.qa)):role/ci
