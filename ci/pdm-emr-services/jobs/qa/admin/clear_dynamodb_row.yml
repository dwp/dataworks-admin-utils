jobs:
  - name: qa-clear-dynamodb-row
    plan:
      - .: (( inject meta-pdm-emr-services.plan.clear-dynamodb-row ))
        config:
          params:
            AWS_ROLE_ARN: arn:aws:iam::((aws_account.qa)):role/ci
            AWS_ACC: ((aws_account.qa))
            CORRELATION_ID: pdm_dataset_generation_pdm_dataset_generation_qa_425
            DATA_PRODUCT: PDM
