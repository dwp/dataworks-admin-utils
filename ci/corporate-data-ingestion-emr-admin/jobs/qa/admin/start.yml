jobs:
  - name: qa-start-cluster
    plan:
      - .: (( inject meta-corporate-data-ingestion-emr-admin.plan.start-cluster ))
        config:
          params:
            AWS_ROLE_ARN: arn:aws:iam::((aws_account.qa)):role/ci
