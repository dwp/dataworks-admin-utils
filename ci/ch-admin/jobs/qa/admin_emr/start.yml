jobs:
  - name: start-emr-cluster-qa
    plan:
      - .: (( inject meta-ch-admin.plan.start-emr-cluster))
        config:
          params:
            AWS_ACC: ((aws_account.qa))
            AWS_ROLE_ARN: arn:aws:iam::((aws_account.qa)):role/ci
