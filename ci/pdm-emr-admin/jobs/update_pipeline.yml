jobs:
  - name: update-pipeline
    plan:
      - get: dataworks-admin_emr-utils
        resource: dataworks-admin_emr-utils-update-pipeline
        trigger: true
      - .: (( inject meta.plan.update-pipeline ))
        config:
          params:
            aviator_filename: aviator-pdm-emr-admin_emr.yml

