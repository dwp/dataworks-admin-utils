jobs:
  - name: start-cluster-production
    plan:
      - .: (( inject meta-kickstart-adg-emr-admin_emr.plan.start-cluster-with-payment))
        config:
          params:
            AWS_ACC: ((aws_account.production))
            AWS_ROLE_ARN: arn:aws:iam::((aws_account.production)):role/ci
            START_DT: "2021-12-23"
            END_DT: "2021-12-23"
            SKIP_FLAG: "True"
            SKIP_MODULES: "payment, application"
