jobs:
  - name: stop-cluster-development
    plan:
      - .: (( inject meta-kickstart-adg-emr-admin_emr.plan.stop-cluster ))
        config:
          params:
            AWS_ACC: ((aws_account.development))
            AWS_ROLE_ARN: arn:aws:iam::((aws_account.development)):role/ci
