spruce:
  - base: ci/lambda-cleanup/pipeline.yml
    to: aviator_pipeline_lambda_cleanup.yml
fly:
  name: lambda-cleanup
  target: aws-concourse
  config: aviator_pipeline_lambda_cleanup.yml
  expose: true
